message = input(">")
words = message.split(" ")# goes thru string and spearates string .
emojis = {
    ":)": "😊",
    ":(": "😒"
}
output = " "
for word in words:
    output += emojis.get(word, word) + " "
print(output)
